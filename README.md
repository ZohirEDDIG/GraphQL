# GraphQL Playground

This repository is for learning and experimenting with GraphQL.

- `main` branch: GraphQL basics (schema, queries, mutations, resolvers)
- `todo-app` branch: Simple Todo App to practice GraphQL CRUD operations with MERN

For learning purposes only.